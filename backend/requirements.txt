# Core
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1

# Database (SQLite zero config)
sqlalchemy[asyncio]==2.0.30
aiosqlite==0.20.0

# HTTP client (todos os providers AI usam httpx)
httpx==0.27.0

# OpenAI (opcional — só necessário se OPENAI_API_KEY estiver no .env)
openai==1.30.1

# Voz gratuita (sem chave)
gtts==2.5.1

# Uploads
python-multipart==0.0.9
